package com.greatlearning.service;

import java.util.Random;

import com.greatlearning.model.Employee;

public class CredentialService {
    public char[] generatedPassword() {
    	String capitalLeters = "ABCDEFGHIJKLMNOPQRSTUVWXYZ";
    	String smallLetters = "abcdefghijklmnopqrstuvwxyz";
    	String numbers = "0123456789";
    	String specialCharacters = "!@#$%^&*<>?+=_.,)";
    	String capitalLetters;
		String value = capitalLetters + smallLetters + specialCharacters ;
    	String values = capitalLetters + smallLetters + numbers + specialCharacters;
    	Random random = new Random ();
    	char [] password = new char [8];
    	for (int =0; i<8; i++) {
    		password [i] = values.charAt(random.nextInt(values.length()));
    	}
    	
    		
    	}
	}
    public String generateEmailAddsress(String firstName , String lastName , String Department) {
    }
    public void showCredentials(Employee employee, String email, char [] generatedPassword ){
	}

}
